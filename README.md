# Автоответчик для Telegram

Проект представляет собой систему автоответчика для Telegram, состоящую из двух основных компонентов: бота для управления настройками и клиента для обработки входящих сообщений.

## Структура проекта

### 1. bot.py - Бот для управления настройками
- Управление статусом автоответчика (запуск/остановка)
- Настройка режимов работы (автоответчик/GPT)
- Выбор чатов для работы
- Настройка текста автоответчика и GPT промптов
- Интерактивное меню с кнопками

### 2. main.py - Клиент для обработки сообщений
- Обработка входящих сообщений в реальном времени
- Поддержка двух режимов работы:
  - **Автоответчик** - отправка заранее заданного текста
  - **GPT режим** - интеллектуальные ответы с использованием OpenAI API
- Поддержка выбора конкретных чатов или всех личных чатов

## Функциональные возможности

### Управление через бота:
- 🚀 Запуск/остановка автоответчика
- 💬 Выбор чатов для работы (конкретные или все)
- 📑 Выбор режима работы
- 🔧 Настройка текста ответов
- 🧠 Настройка GPT промптов

### Режимы работы:
- **Автоответчик** - отправка фиксированного текста
- **GPT режим** - интеллектуальные ответы с сохранением контекста диалога

## Установка и настройка

### Требования
Создайте файл `requirements.txt` со следующим содержанием:

```txt
telebot==0.0.5
python-telegram-bot
mysql-connector-python==8.1.0
openai==1.3.0
telethon==1.28.5
logging
asyncio
```

### Настройка базы данных
Создайте таблицу в MySQL:
```sql
CREATE TABLE autosend (
    id BIGINT PRIMARY KEY,
    status VARCHAR(10) DEFAULT 'stop',
    mode VARCHAR(20) DEFAULT 'autoresponder',
    chats TEXT,
    GPT TEXT,
    autoresponder TEXT
);
```

### Конфигурация

#### bot.py
Замените следующие параметры:
- `BOT_TOKEN` - токен вашего Telegram бота
- Параметры подключения к БД:
  - `HOST`, `USER`, `PORT`, `PASSWORD`, `DATABASE`

#### main.py
Замените следующие параметры:
- `API_ID`, `API_HASH` - из https://my.telegram.org
- `PHONE_NUMBER` - номер телефона аккаунта
- `API_KEY`, `URL`, `MODEL` - параметры OpenAI API
- Параметры подключения к БД

## Запуск

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Запустите бота для управления:
```bash
python bot.py
```

3. Запустите клиент для обработки сообщений:
```bash
python main.py
```

## Использование

1. Начните работу с ботом командой `/start`
2. Настройте необходимые параметры через меню:
   - Выберите чаты для работы
   - Выберите режим работы
   - Настройте текст ответов или GPT промпты
3. Запустите автоответчик кнопкой "🚀 Запустить"

## Особенности

- 💾 Настройки сохраняются в базе данных
- 🔒 Доступ только для авторизованных пользователей
- 📝 Подробное логирование всех операций
- 🔄 Автоматическое переподключение к БД при ошибках
- 💬 Поддержка контекстных диалогов в GPT режиме

## Безопасность

- Проверка прав доступа пользователей
- Обработка ошибок с подробным логированием
- Безопасное хранение настроек в БД

## Логирование

Проект использует расширенную систему логирования с разделением на stdout и stderr для разных уровней сообщений.
